server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/identity_test
    username: identity
    password: identity
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    clean-disabled: false

app:
  jwt:
    secret: test-jwt-secret-key-that-is-at-least-32-chars-long
    access-token-ttl: 15m
    refresh-token-ttl: 7d
  swagger:
    public: true
  admin:
    email: admin@eventplatform.local
    password: AdminPass123!
    first-name: Platform
    last-name: Admin
